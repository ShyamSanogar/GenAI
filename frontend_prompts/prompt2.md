Generate a single-file React component (using Tailwind for styling) that:
- Fetches courses from GET http://localhost:3000/api/courses
- Displays them as cards showing title, instructor, price
- Has a search input that filters by title client-side
- Has a "Login" form that POSTs to /api/auth/login and stores the returned JWT
- Uses fetch, not axios
- No external UI libraries beyond React + Tailwind

Return only the code, no explanation.